# Overlock MCP Server Configuration

# gRPC Configuration  
OVERLOCK_GRPC_URL=localhost:9090
OVERLOCK_API_TIMEOUT=30s

# Server Configuration  
MCP_HTTP_ADDR=127.0.0.1:8080

# Optional: Enable debug logging
DEBUG=false